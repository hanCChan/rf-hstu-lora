% Fig.2 — single clean architecture diagram (matrix rows, no subfigures)
\usetikzlibrary{matrix,fit,calc,backgrounds,positioning}

\begin{tikzpicture}[
    font=\footnotesize,
    >=Latex,
    node distance=0.40cm and 0.38cm,
    box/.style={
        draw,
        rounded corners=1.6pt,
        minimum height=0.68cm,
        minimum width=1.05cm,
        align=center,
        inner sep=2.5pt,
        line width=0.45pt
    },
    fuse/.style={box, fill=gray!14, minimum width=1.18cm},
    arr/.style={->, line width=0.45pt},
    lbl/.style={font=\scriptsize\bfseries, anchor=west},
    tag/.style={font=\scriptsize, align=center}
]

% ---- Row 1: Main RF path ----
\node[lbl] (r1l) at (0, 1.05) {Main RF path};
\node[box, right=0.55cm of r1l] (iq) {IQ/FFT/AP};
\node[box, right=of iq] (cnn) {CNN stem};
\node[box, right=of cnn] (tm) {Main tokens\\$T_m$};
\node[box, right=of tm] (pos) {Pos./chirp};
\node[fuse, right=1.05cm of pos] (fusion) {OOB fusion\\module};
\node[box, right=of fusion] (tf) {Fused\\$T_f$};
\node[box, right=of tf] (hstu) {RF-HSTU};
\node[box, right=of hstu] (logits) {Window\\logits};
\node[box, right=of logits] (vote) {Mean\\$K$ win.};
\node[box, right=of vote] (dec) {File-level\\$\hat{y}$};

\draw[arr] (iq) -- (cnn);
\draw[arr] (cnn) -- (tm);
\draw[arr] (tm) -- (pos);
\draw[arr] (pos.east) -- ++(0.28,0) |- (fusion.west);
\draw[arr] (fusion) -- (tf);
\draw[arr] (tf) -- (hstu);
\draw[arr] (hstu) -- (logits);
\draw[arr] (logits) -- (vote);
\draw[arr] (vote) -- (dec);

\node[tag, below=0.04cm of tm] {$P{=}32$, $D{=}64$};

% ---- Row 2: OOB path ----
\node[lbl] (r2l) at (0, -0.05) {OOB path};
\node[box, right=0.55cm of r2l] (oobs) {OOB\\spectrum};
\node[box, right=of oobs] (oobtok) {OOB\\tokenizer};
\node[box, right=of oobtok] (to) {OOB tokens\\$T_o$};

\draw[arr] (oobs) -- (oobtok);
\draw[arr] (oobtok) -- (to);

% Q / K,V routing (edge-to-edge, no text overlap)
\draw[arr] (tm.south) -- ++(0,-0.22) -| node[pos=0.22, left, tag] {$Q$} (fusion.south);
\draw[arr] (to.east) -- ++(0.55,0) |- node[pos=0.18, below, tag] {$K,V$} (fusion.south);

% ---- Row 3: Fusion zoom-in callout ----
\node[lbl] (r3l) at (0, -1.15) {Fusion detail};
\begin{scope}[shift={($(r3l.east)+(0.55,0)$)}]
    \node[box, anchor=west] (q) at (0,0) {$Q{=}T_m$};
    \node[box, right=0.42cm of q] (kv) {$K,V{=}T_o$};
    \node[fuse, right=0.42cm of kv] (mha) {MHA};
    \node[fuse, right=0.42cm of mha] (gate) {Gate $G$};
    \node[box, right=0.42cm of gate] (ln) {LN};
    \node[box, right=0.42cm of ln] (tout) {$T_f$};

    \draw[arr] (q) -- (mha);
    \draw[arr] (kv) -- (mha);
    \draw[arr] (mha) -- (gate);
    \draw[arr] (gate) -- (ln);
    \draw[arr] (ln) -- (tout);

    % Residual skip above boxes (arc path, not through text)
    \draw[arr, dashed]
        (q.north) ++(0,0.18) -- ++(5.85,0) -- (ln.north)
        node[midway, above=0.04cm, tag] {residual from $T_m$};

    \node[tag, below=0.06cm of gate] {$T_f=\mathrm{LN}(T_m+G\odot AW_o)$};
\end{scope}

% Light row backgrounds (no tight dashed stage boxes)
\begin{scope}[on background layer]
    \fill[gray!6]  ($(r1l.north west)+(-0.08,0.14)$) rectangle ($(dec.north east)+(0.12,-0.52)$);
    \fill[gray!10] ($(r2l.south west)+(-0.08,0.52)$) rectangle ($(to.south east)+(0.12,-0.14)$);
    \fill[gray!14] ($(r3l.south west)+(-0.08,0.52)$) rectangle ($(tout.south east)+(0.55,-0.18)$);
\end{scope}

% Callout bracket linking fusion module to detail row
\draw[line width=0.4pt]
    (fusion.south) -- ++(0,-0.12) -| ($(r3l.east)+(0.2,0)$);

\end{tikzpicture}
