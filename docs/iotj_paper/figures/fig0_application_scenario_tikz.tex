% Gateway-side LoRa RFFI application scenario (TikZ)
\begin{tikzpicture}[
    font=\small,
    >=Latex,
    node distance=0.42cm and 0.62cm,
    box/.style={
        draw,
        rounded corners=2pt,
        minimum height=0.82cm,
        minimum width=1.35cm,
        align=center,
        line width=0.45pt,
        inner sep=3pt
    },
    devbox/.style={box, fill=blue!8},
    gwbox/.style={box, fill=gray!10},
    authbox/.style={box, fill=green!9},
    outbox/.style={box, fill=orange!10},
    zone/.style={
        draw,
        rounded corners=3pt,
        dashed,
        line width=0.35pt,
        inner sep=7pt,
        fill opacity=0.15,
        text opacity=1
    },
    arr/.style={->, line width=0.48pt},
    shift/.style={font=\scriptsize, align=center, fill=white, inner sep=1.5pt},
    note/.style={font=\footnotesize, align=center}
]

% ----- Devices -----
\node[devbox] (enrolled) {Enrolled\\LoRa devices};
\node[devbox, below=0.35cm of enrolled] (rogue) {Suspicious /\\cloned device};

% ----- Wireless link + gateway -----
\node[gwbox, right=1.15cm of enrolled, minimum width=1.55cm] (channel) {Deployment-varying\\wireless channel};
\node[gwbox, right=0.85cm of channel, minimum width=1.45cm] (gateway) {LoRa gateway\\receiver};

\draw[arr] (enrolled.east) -- ++(0.28,0) |- (channel.west);
\draw[arr] (rogue.east) -- ++(0.28,0) |- (channel.west);
\draw[arr] (channel) -- node[above, note, pos=0.5] {LoRa packets} (gateway);

% Shift labels around wireless link
\node[shift, above=0.18cm of channel] (sday) {day drift};
\node[shift, below=0.18cm of channel] (sdist) {distance / location};
\node[shift, above left=0.08cm and 0.02cm of channel] (scfg) {configuration};
\node[shift, below right=0.08cm and 0.02cm of channel] (srx) {receiver front-end};

% ----- Authentication pipeline -----
\node[authbox, right=0.85cm of gateway, minimum width=1.55cm] (capture) {IQ windows +\\spectral/OOB views};
\node[authbox, right=0.75cm of capture, minimum width=1.65cm] (model) {OOB-guided\\RF-HSTU auth};
\node[outbox, right=0.75cm of model, minimum width=1.35cm] (accept) {Accept\\enrolled ID};
\node[outbox, below=0.32cm of accept, minimum width=1.35cm] (alert) {Flag\\suspicious TX};

\draw[arr] (gateway) -- (capture);
\draw[arr] (capture) -- (model);
\draw[arr] (model.east) -- ++(0.32,0) |- (accept.west);
\draw[arr] (model.east) -- ++(0.32,0) |- (alert.west);

% ----- Zone boxes -----
\node[zone=fill=blue!12, fit=(enrolled)(rogue),
      label={[font=\small\bfseries]above:Transmitters}] {};
\node[zone=fill=gray!18, fit=(channel)(gateway)(sday)(sdist)(scfg)(srx),
      label={[font=\small\bfseries]above:Gateway RF capture}] {};
\node[zone=fill=green!18, fit=(capture)(model)(accept)(alert),
      label={[font=\small\bfseries]above:Authentication decision}] {};

\end{tikzpicture}
